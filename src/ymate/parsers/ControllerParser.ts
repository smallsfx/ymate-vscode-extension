import * as vscode from 'vscode';
import { Controller, ControllerInterface } from "../models/Controller";
/**
 * 表示一个控制器解析对象的实例
 * @author smalls
 */
export class ControllerParser {
  /** 解析目录中的控制器
   * @param path 需要解析的目录
   */
  static parse(path: string): Controller[] {
    let models: Controller[] = [];

    return models;
  }

}