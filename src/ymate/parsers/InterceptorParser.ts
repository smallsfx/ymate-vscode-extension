import * as vscode from 'vscode';
import {ymate,views,DataType} from '../';

/**
 * 解析文本内容，生成拦截器实体对象并且返回
 * @param document 文本所属文档
 * @author smalls
 */
export function InterceptorParser(document: vscode.TextDocument): views.TreeNode {
  let _treeNode: views.TreeNode;

  return _treeNode;
}
