import * as vscode from 'vscode';
import { Module } from "../models/Module";
/**
 * 表示一个模块解析对象的实例
 * @author smalls
 */
export class ModuleParser {
  /** 解析目录中的模块
   * @param path 需要解析模块的目录
   */
  static parse(path: string): Module[] {
    let models: Module[] = [];

    return models;
  }

}